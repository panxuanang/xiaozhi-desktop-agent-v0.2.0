# Office XLSX fallback skill

Use openpyxl/XlsxWriter. Apply consistent fonts, styled headers, wrap text, sensible column widths and row heights, freeze panes, filters, borders, top alignment, and avoid special glyphs that cause font fallback. Reopen the final workbook with openpyxl before delivery and verify critical numbers.
